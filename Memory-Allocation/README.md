# Dynamic Memory Allocation in C

A simple C program demonstrating dynamic memory allocation using `malloc()`.

## 📝 Features
- **Dynamic Allocation:** Allocates space on the **Heap** at runtime based on user input.
- **Error Handling:** Safe `NULL` check to prevent crashes if allocation fails.
- **Memory Safety:** Uses `free()` to prevent memory leaks and sets the pointer to `NULL`.

